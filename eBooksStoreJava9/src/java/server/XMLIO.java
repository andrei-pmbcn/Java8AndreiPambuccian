/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package server;

/**
 *
 * @author andrei
 */
public class XMLIO {
    static String filepath = "/home/andrei/NetBeansProjects/Java9AndreiPambuccian/eBooksStoreJava9/src/java/xml/users.xml";
    
    public static void genTables() {
/*        
        FileOutputStream out= null;
        try {
                out = new FileOutputStream(filepath);
                // create an int variable to be loaded from input file and to be written to output stream
                int val;
                // read the int value from input stream and write it to output stream
                while ((val = in.read()) != -1) {
                    out.write(val);
                }
        } catch (FileNotFoundException ex) {
                Logger.getLogger(InputAndOutputStreamsAsFiles.class.getName()).log(Level.SEVERE, null, ex);
        }
        catch (IOException ex) {
                Logger.getLogger(InputAndOutputStreamsAsFiles.class.getName()).log(Level.SEVERE, null, ex);
        }
        finally {
            if (in != null) {
                    in.close();
            }
            if (out != null) {
                    out.close();
            }
        }

  */      
    }
    
}
